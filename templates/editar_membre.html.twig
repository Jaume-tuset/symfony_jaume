{% extends 'base.html.twig' %}
{% block title %}Editar Membre{% endblock %}
{% block stylesheets %}
{{ parent() }}
 <link href="{{ asset('assets/css/estilsEquips.css') }}" rel="stylesheet">
{% endblock %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block body %}
    {% if error %}
        <div class="exception">{{error}}</div>
    {% endif %}
    <h1 id="titolInici">Editar Membre</h1>
    <div id="paragrafInici">
        {{ form_start(formulari) }}
        {{ form_widget(formulari) }}
        {{ form_end(formulari) }}
    </div>
    <div id="paragrafInici">  
        <img class="imatgeInici" src="{{ asset('assets/img/membres/'~imatge) }}">
    </div>
    <div id="paragrafInici">  
        <a class="nav-link" href ="{{ path('inici') }}">Tornar a l'inici</a>
    </div>
{% endblock %}

{% block footer %}
{{ parent() }}
{%endblock%}