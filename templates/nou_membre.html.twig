{% extends 'base.html.twig' %}
{% block title %}Añadir equipo{% endblock %}
{% block stylesheets %}
{{ parent() }}
 <link href="{{ asset('assets/css/estilsEquips.css') }}" rel="stylesheet">
{% endblock %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
{% block body %}
    {% if error %}
        {{ form(form) }}
        <div class="exception">{{error}}</div>
    {% endif %}
    <h1 id="titolInici">Nou Membre</h1>
    <div id="paragrafInici">
        {{ form(form) }}
        {{ form_start(formulari) }}
        {{ form_widget(formulari) }}
        {{ form_end(formulari) }}
    </div>
    <div id="paragrafInici">
        <a class="nav-link" href ="{{ path('inici') }}">Tornar a l'inici</a>
    </div>
{% endblock %}

{% block footer %}
{{ parent() }}
{%endblock%}